# unlike strings list are muatable
# list is a collection of values, you can have different types of items in the list
# we can add , remove from a list not from string

letter = ["apple", "banana", 23, 45.9]

print(letter[0])

# updating the 0 index
letter[0] = "Grapes"
print(letter[0])

print(letter)
# we can slice the list just like strings